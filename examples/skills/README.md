# examples/skills/

Reference skill implementations that a third party (or future Volmarr) can fork to learn the skill contract.

Each example is a complete, runnable skill — never a snippet, never a "this is what it would look like" sketch. CI runs every example skill against the current `src/runa/skills/` loader.
