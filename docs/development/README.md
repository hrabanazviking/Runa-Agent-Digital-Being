# docs/development/

How a contributor (human or AI agent) works on Runa's code.

## Planned canonical documents

- `CONTRIBUTING.md` — branch model, PR review rules, commit message conventions, the Mythic Engineering Forge Cycle as it applies here.
- `LOCAL_SETUP.md` — getting a working dev environment on Linux / macOS / Windows / WSL.
- `TESTING.md` — running the suite, writing new tests, the per-tier rules (unit / integration / e2e).
- `STYLE.md` — coding style specific to Runa beyond what `ruff` enforces.
- `WORKING_WITH_AI_AGENTS.md` — how Volmarr's AI collaborators (Hermes, Codex, Claude Code subagents, Runa herself once self-modifying) work in this repo.

Currently empty. To be populated as `src/runa/` actually fills in.
