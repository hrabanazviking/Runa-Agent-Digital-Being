# scripts/dev/

Developer convenience scripts: format, lint, full-test, regenerate snapshots, rebuild lockfile, refresh fixtures.

All are safe to run repeatedly. None touch a deployed Runa or production data.
