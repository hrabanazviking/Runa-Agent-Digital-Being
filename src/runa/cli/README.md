# src/runa/cli/

The `runa` command-line entry point.

Thin parser + dispatcher. All actual behaviour lives in `runa.runtime` and below. If you find yourself doing real work in this folder, move it.

See `INTERFACE.md` for the public surface.
