# tests/fixtures/

Reusable test data: seed memories, sample conversations, mock model responses, sample world-model snapshots, example skill packages.

Fixtures are loaded by tests across all tiers. Treat them as a small read-only library — add new fixtures by adding new files, don't mutate existing ones.
