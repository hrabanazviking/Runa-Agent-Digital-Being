# deploy/examples/

Reference deployment recipes. Each subfolder is one named deployment shape with the exact files needed to reproduce it.

Planned recipes:
- `single-pi/` — Runa lives entirely on one Pi 5.
- `pi-and-laptop/` — Pi runs core, laptop runs heavy model inference.
- `longhall/` — multiple machines on a Tailnet, services split by role.
